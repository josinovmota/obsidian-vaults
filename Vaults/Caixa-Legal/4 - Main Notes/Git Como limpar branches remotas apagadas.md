Você chega pra trabalhar e descobre que a branch `origin/feature/answers` foi apagada, mas no seu **Computador** ela ainda existe. Para resolver isso usamos o comando seguinte:

```shell
git remote prune origin
```

É importante lembrar que a **Origin** não necessariamente é **Origin**. Pode ser que no projeto esteja sendo usada outra referência